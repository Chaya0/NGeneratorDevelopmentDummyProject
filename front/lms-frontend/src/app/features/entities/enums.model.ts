export enum CheckoutStatus {
	BORROWED = 'BORROWED',
	RETURNED = 'RETURNED',
	OVERDUE = 'OVERDUE',
}

export enum ReservationStatus {
	PENDING = 'PENDING',
	FULFILLED = 'FULFILLED',
	CANCELLED = 'CANCELLED',
}

